---
- name: Install go-server
  apt:
    name: "{{ item }}"
    update_cache: 'yes'
    state: present
  become: true
 

- name: Enable service go-server
  systemd:
    enabled: true
    name: go-server
  become: true

- name: Start service go-server
  systemd:
    state: started
    name: go-server
  become: true
